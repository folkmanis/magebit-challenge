/* import JustValidate from "just-validate";

const validator = new JustValidate("#customer-login-form");

validator
    .addField("email", [
        {
            rule: "required",
        },
        {
            rule: "email",
        },
    ])
    .addField("pass", [
        {
            rule: "required",
        },
    ]);
 */
